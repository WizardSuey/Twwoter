<div class="d-flex justify-content-between align-items-center">
    <h1>Занятия</h1>
    <% if Current.user.vip? || Current.user.email = 'dorogino@gmail.com' || Current.user.email = 'amigo2542@gmail.com' %>
        <%= link_to 'Добавить занятие', new_tweet_path, class: 'btn btn-primary' %>
    <% end %>
</div>

<div class="mb3">
<%= render 'tweets' %>
</div>
